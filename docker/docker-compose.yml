services:
  mock-github:
    build: ./mock-github
    ports:
      - "4000:4000"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:4000/health"]
      interval: 2s
      timeout: 5s
      retries: 3
      start_period: 2s
