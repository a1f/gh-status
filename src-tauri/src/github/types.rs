//! GitHub API response DTOs.
//!
//! These map directly to the GraphQL/REST response shapes
//! and get converted into our domain types in client.rs.
//! Kept separate from domain types (crate::types) because
//! API shapes change independently of our internal model.

// TODO: populate with GraphQL response structs in M1.3
